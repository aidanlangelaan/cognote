# Session state is managed within the record process lifetime.
# No cross-process IPC is needed since Ctrl+C stops and transcribes in one process.
# This module is reserved for future use (e.g. crash recovery).
